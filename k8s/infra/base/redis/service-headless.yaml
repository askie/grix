apiVersion: v1
kind: Service
metadata:
  name: aibot-redis-headless
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: aibot-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
