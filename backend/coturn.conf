# coturn TURN server config (dev)
# Production: replace with real TLS cert and domain

listening-port=3478
tls-listening-port=5349
realm=grix.dhf.pub
server-name=grix.dhf.pub

# Dev: use plain credentials (no auth db)
lt-cred-mech
user=aibot:aibot_secret

# Log verbosity for dev
verbose
fingerprint

# Allow all origins (dev only)
no-multicast-peers
