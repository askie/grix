apiVersion: v1
kind: ConfigMap
metadata:
  name: aibot-coturn-config
data:
  turnserver.conf: |
    listening-port=3478
    fingerprint
    lt-cred-mech
    no-multicast-peers
    no-tls
    no-dtls
    tls-listening-port=5349
    # relay 端口范围
    min-port=50300
    max-port=50319
