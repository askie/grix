livekit>=0.17.0
livekit-agents>=1.2.9
# 必须锁 1.2.9：volcengine 1.3.0 硬钉 livekit-agents==1.2.9，整栈锁在 agents 1.2.9；
# openai 插件 >=1.6 要 agents>=1.6.1，与火山冲突无法同进程并存。1.2.9 与本栈匹配。
# 注意：1.2.9 的 openai 接口本身已是 GA(SDK 2.x)，非流式转写走 GA HTTP 端点正常；
# 仅"流式实时转写"在 1.2.9 走的是已被官方停用的 beta WS 协议(连即被 GA 端点拒)，
# 故念稿管线用非流式。要流式须升 openai 1.6.x(GA WS 协议)→须拆栈，见记忆 stack_split。
livekit-plugins-openai==1.2.9
# OpenAI 管线用：非流式 STT 无判停，silero 本地 VAD 负责切句+轮次+打断（同锁 1.2.9）。
livekit-plugins-silero==1.2.9
livekit-plugins-volcengine==1.3.0
nats-py>=2.7.0
python-dotenv>=1.0.0
