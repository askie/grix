apiVersion: v1
kind: Service
metadata:
  name: aibot-postgres-ro
spec:
  selector:
    app.kubernetes.io/name: aibot-postgres-ro
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
