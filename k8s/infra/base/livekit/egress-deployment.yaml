apiVersion: apps/v1
kind: Deployment
metadata:
  name: aibot-livekit-egress
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: aibot-livekit-egress
  template:
    metadata:
      labels:
        app.kubernetes.io/name: aibot-livekit-egress
    spec:
      automountServiceAccountToken: false   # Egress 不调用 K8s API，不挂载 SA token
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 30
      containers:
        - name: egress
          image: livekit/egress:v1.11.0
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          env:
            - name: EGRESS_CONFIG_BODY
              valueFrom:
                secretKeyRef:
                  name: aibot-livekit-egress-env
                  key: EGRESS_CONFIG_BODY
            # 以 uid 1000 运行时镜像默认 HOME 不可写（web egress 的 Chrome 需要可写
            # HOME），兜底指向 /tmp
            - name: HOME
              value: /tmp
          ports:
            - name: health
              containerPort: 7981
          readinessProbe:
            httpGet:
              path: /
              port: health
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: health
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: "1"
              memory: 1Gi
            limits:
              cpu: "4"
              memory: 4Gi
